name: INFINITO 3.0 Authorized V6 R2
on:
  push:
    branches: [eval/v6-r2-live-20260915]
permissions:
  contents: read
concurrency:
  group: infinito3-first-frozen-v6
  cancel-in-progress: false
jobs:
  first-v6:
    if: github.event.created == true && github.run_attempt == 1
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
        with:
          ref: befa7636ea2db4e1a70ee235590743dedf11ac8b
          persist-credentials: false
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install frozen direct dependencies
        run: python -m pip install openai==3.14.0 pytest==9.1.1
      - name: Deterministic tests and freeze preflight
        run: |
          python -m pytest -q tests/*_v3.py --disable-warnings
          python scripts/run_infinito3_trajectory_holdout_v6_r2.py
      - name: Single authorized live V6
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
        run: >-
          python scripts/run_infinito3_trajectory_holdout_v6_r2.py
          --authorize-live-v6
          --expected-revision befa7636ea2db4e1a70ee235590743dedf11ac8b
          --output-dir trajectory-holdout-v6-results
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: infinito3-v6-r2-${{ github.run_id }}-${{ github.run_attempt }}
          path: trajectory-holdout-v6-results/
          if-no-files-found: warn
          retention-days: 30
