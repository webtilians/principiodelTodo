name: INFINITO 3.0 Frozen V3 Semantic Event Extractor

on:
  push:
    branches: [eval/v3-semantic-event-extractor-20260914]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  heldout-v3-semantic:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install dependencies
        run: python -m pip install openai pytest
      - name: Deterministic tests before paid run
        run: python -m pytest tests/test_*v3.py -q
      - name: Frozen V3 with semantic cognitive event extractor
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
        run: >-
          python scripts/run_infinito3_trajectory_holdout_v3_semantic.py
          --model gpt-5.6-luna
          --reasoning-effort none
          --embedding-provider openai
          --embedding-model text-embedding-3-small
          --semantic-reranker llm
          --reranker-model gpt-5.6-luna
          --event-model gpt-5.6-luna
          --output-dir trajectory-holdout-v3-semantic-results
      - name: Summary
        if: always()
        run: |
          if [ -d trajectory-holdout-v3-semantic-results ]; then
            find trajectory-holdout-v3-semantic-results -name '*.md' -exec cat {} \; >> "$GITHUB_STEP_SUMMARY"
          fi
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: infinito3-heldout-v3-semantic-extractor
          path: trajectory-holdout-v3-semantic-results/
          if-no-files-found: warn
          retention-days: 30
